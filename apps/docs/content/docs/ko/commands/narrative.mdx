---
title: narrative
description: 커밋에 결속된 프로젝트 지식 상태로 self-contained HTML 리포트를 생성합니다
command_path: narrative
---

## 무엇을 하는 명령인가

`narrative`는 선택된 snapshot history, commit에 결속된 문서, artifact record, event data를 읽어 self-contained HTML narrative report를 생성합니다. 이 리포트는 협업 과정을 처음부터 다시 훑지 않아도 프로젝트의 의도, 결정, 시간축을 이해해야 하는 신규 참여자나 에이전트를 위한 것입니다.

## 언제 쓰는가 / 언제 쓰지 않는가

### 언제 쓰는가

- 프로젝트 결정이 어떻게 바뀌었는지 손쉽게 인수인계하고 싶을 때
- raw log가 아니라 이야기 구조로 프로젝트 지식을 설명하고 싶을 때
- CLI 밖에서도 열 수 있는 독립 HTML 산출물이 필요할 때

### 언제 쓰지 않는가

- 현재 저장소 요약만 필요할 때. <a href="/ko/docs/commands/status">`status`</a>를 사용하십시오.
- snapshot semantic delta가 필요할 때. <a href="/ko/docs/commands/log">`log`</a>를 사용하십시오.
- append-only 운영 이벤트가 필요할 때. <a href="/ko/docs/commands/timeline">`timeline`</a>을 사용하십시오.
- retrieval hit나 즉답형 질의가 필요할 때. <a href="/ko/docs/commands/query">`query`</a>를 사용하십시오.

## 기본 구문

```bash
pnpm ragit narrative [revRange] [--max-commits <n>] [--output <path>] [--open] [--dry-run] [--format text|json|both] [--cwd <path>]
```

## 인자와 옵션

- `[revRange]`: 선택적 git revision range입니다. 생략하면 `HEAD`를 사용합니다.
- `--max-commits <n>`: 선택되는 snapshot commit 수를 제한합니다. 기본값은 `10`입니다.
- `--output <path>`: 기본값 `.ragit/reports/narrative/<anchorSha>.html` 대신 지정 경로로 HTML 리포트를 저장합니다.
- `--open`: 생성된 리포트를 저장 후 기본 브라우저로 엽니다.
- `--dry-run`: 파일을 쓰지 않고 리포트 계획과 요약만 계산합니다.
- `--format text|json|both`: 사람이 읽는 text, JSON, 또는 둘 다를 선택합니다.
- `--cwd <path>`: 다른 저장소를 대상으로 실행합니다.

## 입력/출력 계약

- JSON payload 입력은 없습니다.
- JSON 출력에는 `dryRun`, `reportPath`, `headSha`, `window`, `summary`, `warnings`가 포함됩니다.
- `window`에는 `revRange`, `maxCommits`, `selectedSnapshotShas`, `missingSnapshotCommits`가 들어 있습니다.
- `--dry-run`도 planned report path와 summary는 반환합니다.
- `--open`은 dry-run에서는 무시되고 warning으로만 보고됩니다.
- 선택된 window에 manifest가 붙은 snapshot이 하나도 없어도, `narrative`는 empty-state report를 생성합니다.

## 대표 사용 예시

기본 narrative report 생성:

```bash
pnpm ragit narrative
```

window를 제한하고 결과를 열기:

```bash
pnpm ragit narrative HEAD~20..HEAD --max-commits 5 --open
```

JSON으로 report plan 미리 보기:

```bash
pnpm ragit narrative --dry-run --format json
```

## 실패/주의 사항

- `--output`은 파일을 쓰므로 상위 디렉터리에 쓰기 권한이 있어야 합니다.
- 이 리포트는 raw transcript가 아니라 snapshot-backed knowledge를 기반으로 생성됩니다.
- `--open`은 best-effort이며, 현재 머신에 opener가 없으면 건너뛸 수 있습니다.
- 선택된 window에 snapshot manifest가 없더라도 command는 실패하지 않고 empty-state report로 성공합니다.

## 관련 명령

- <a href="/ko/docs/commands/status">`status`</a>
- <a href="/ko/docs/commands/log">`log`</a>
- <a href="/ko/docs/commands/timeline">`timeline`</a>
- <a href="/ko/docs/commands/query">`query`</a>
- <a href="/ko/docs/commands/drift">`drift`</a>
- <a href="/ko/docs/commands/repair">`repair`</a>
