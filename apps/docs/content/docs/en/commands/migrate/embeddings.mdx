---
title: migrate embeddings
description: Rebuild the canonical store against the configured embedding contract
command_path: migrate embeddings
---

## What It Does

`migrate embeddings` rebuilds `.ragit/store` from the manifests that already exist in the repository, using the embedding provider contract described by the current config.

## When To Use / When Not To Use

### When to use it

- You changed `embedding.provider`, `embedding.model`, or local placeholder dimensions/version.
- <a href="/en/docs/commands/status">`status`</a> reports `embedding.needsMigration=true`.
- You need to move the same snapshot corpus onto a different embedding contract without rewriting manifests.

### When not to use it

- You only changed document content. Use <a href="/en/docs/commands/ingest">`ingest`</a>.
- You are importing a legacy store. Use the other `migrate` subcommands instead.

## Syntax

```bash
pnpm ragit migrate embeddings [--dry-run] [--cwd <path>]
```

## Arguments And Options

- `--dry-run`: Return the current contract, target contract, and manifest/doc/chunk counts without rewriting the store.
- `--cwd <path>`: Run against another repository.

## Input And Output Contract

- There is no positional input and no JSON payload input.
- Output is JSON and includes `mode`, `currentContract`, `targetContract`, `manifests`, `documents`, `chunks`, and `migrationNeeded`.
- The command rewrites `.ragit/store` only on apply. Manifest files stay unchanged.

## Examples

After changing a provider or model, inspect the embedding state first:

```bash
pnpm ragit status --format json
```

When `embedding.needsMigration=true`, preview the migration before applying it:

```bash
pnpm ragit migrate embeddings --dry-run
```

Apply only after reviewing the preview:

```bash
pnpm ragit migrate embeddings
```

## Failures And Cautions

- The source store must still contain every chunk referenced by the manifests. Missing chunks fail fast.
- The command creates `.ragit/store.next` and `.ragit/store.prev` during the swap.
- Provider credentials are still env-only. An OpenAI apply requires `OPENAI_API_KEY` to already be present in the command environment; there is no silent fallback from a requested provider to `local-placeholder`. OpenAI is not production-supported by this release without authorized live evidence.
- If `init` used the safe gitignore policy, manifests may be local-only. Share or track `.ragit/manifest/**` with the `snapshot-history` or `dogfood` policy when other machines must reproduce the same snapshot corpus.

## Related Commands

- <a href="/en/docs/commands/status">`status`</a>
- <a href="/en/docs/commands/doctor">`doctor`</a>
- <a href="/en/docs/commands/config/set">`config set`</a>
